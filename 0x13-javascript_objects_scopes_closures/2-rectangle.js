#!/usr/bin/node

class Rectangle {
    h;
    w;
    
    constructor(h, w){
        this.width = w;
        this.height = h;

    }
    if (w === 0 || w <= 0 || h === 0 || h <= 0){
        empty_obj = Rectangle();

    }
        

}